# opcda-bridge environment

# Gateway
OPC_BRIDGE_PORT=7600

# Test environment (OPC DA server for smoke testing)
OPC_TEST_HOST=
OPC_TEST_SERVER=
OPC_TEST_TAG=

# SonarCloud API access for local issue verification
SONAR_TOKEN=
