[package]
name = "opcda-bridge-client"
version = "0.1.4"
edition = "2024"
description = "OPC DA cross-platform client"
license = "MIT"
repository = "https://github.com/mikeboiko/opcda-bridge"

[dependencies]
bridge-client-core = { path = "../bridge-client-core", version = "0.1.0" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
anyhow = "1"
clap = { version = "4", features = ["derive", "env"] }
tabled = "0.19"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "1"

[dev-dependencies]
bridge-proto = { path = "../bridge-proto", version = "0.1.0" }
tonic = "0.14"
tonic-prost = "0.14"
tokio-stream = "0.1"
tempfile = "3"

[package.metadata.cargo-machete]
ignored = ["tonic-prost"]
